# roadmap — 踩地雷 Minesweeper

更新:**2026-09-16 晚**(agape250 機・Opus 5・0916-hfpc-minesweeper 場)
線上 v6:https://hfpc-minesweeper.summer09201017.workers.dev

---

## ✅ 已完成(別重做)

### v1 WinXP 原味 + 無猜盤面
- 三難度(高級是 **30 寬×16 高**)+ 自訂盤、和弦展開(旗插錯會炸,不幫玩家擋)、問號標記、
  笑臉四態、七段顯示器、數字八色照原版、圖示全手繪 SVG(不用 emoji)
- **無猜盤面**:第一格按下去之後才佈雷 ⇒ 生成器知道起點,一直重抽到抽出「每一步都推得出來」為止。
  生不出來時**誠實標成「⚠ 一般盤面:可能需要猜」**
- 每日挑戰 `?daily` / 題號 `?seed=`(連第一格都由種子決定)、三難度個人紀錄、3BV 與效率

### v2 換皮 / 換背景 / 背景音樂
- 主題 7 種(含**高對比投影用**)、背景、三首原創零音檔 BGM(預設關)

### v3 場景背景
- 8 張向量場景(草原藍天/星空夜色/月夜森林/夕陽/98 青綠/素灰/牛皮紙),零圖檔、離線可用

### v4 教練層
- 💡 提示(H)、↩ 悔一步 3 次(U,踩雷可復活)、⌨ 全鍵盤操作、
  💥 死因分析(推得出來?有別的安全格?還是只能猜)、🤖 教學回放
- 📊 統計漏斗補齊:`-start` / `-lost-done` / `-quit-done` ⇒ **放棄率 = quit ÷ start**

### v5 裝到手機主畫面
- PNG 圖示 192/512/maskable/apple-touch-icon(**iOS 只吃 PNG**)、站內安裝橫幅與選單入口、
  三種平台三種說法(Android 直接 prompt / iPhone 教分享選單 / LINE 明講要換瀏覽器)

### v6 沉浸模式與手機版面
- ⛶ = 收自己的殼 +(拿得到的話)全螢幕;手機直向格子 34→39px;底部列不再排兩列;
  轉向提示改成用算的
- `scripts/screen-budget.mjs` 螢幕預算量尺(四個指標,可對任何網址跑)

---

## 🔜 待做(CP = 價值 ÷ 開發時間,由高到低)

| # | 項目 | ⏱ | ★ | 為什麼 |
|---|---|---|---|---|
| 1 | 🖼 **每日挑戰分享圖卡** | ~1h | ★★★★ | 打磚塊 0916 已做過一份純 canvas 1080×1440 的可抄(含 Web Share→下載→複製三層退路)。現在玩完**沒東西可炫耀**,回訪動機少一截 |
| 2 | 🧪 **練功房無限題 + 每日三軌** | ~2h | ★★★★ | `?seed=` 已經通了,只差 UI。**老師報題號、全班同一盤**是主日學最實用的一件 |
| 3 | 🏫 **課堂模式** | ~2–3h | ★★★ | 大字 + 分組計分 + 投影友善;`lesson-mode`/`classroom-*` skill 現成,搭「高對比」主題可直接上課 |
| 4 | 📱 手機高級盤雙指縮放/平移 | ~3h | ★★ | 30×16 在手機上沒有解(已量過),縮放是唯一能讓它「能看」的路 |
| 5 | 🏆 成就系統 | ~2h | ★★ | 資料都在 records 裡了,純 UI;給孩子短期目標 |
| 6 | 🔔 統計回讀:放棄率出來之後再決定 | — | ★★★★★ | **見下面「等數據」** |

### ⏳ 等數據(不要現在猜)

`-start` / `-lost-done` / `-quit-done` 是 2026-09-16 才上線的。**再過 3~7 天** `/stats` 就能回答:

- 放棄的人**平均活了幾秒**?
- 「開局三秒就走」(=看不懂怎麼玩 → 該做開場教學卡)
- 還是「玩了兩分鐘卡住才走」(=提示鈕不夠明顯,或預設難度太高)

**這兩種指向完全不同的下一個功能。** 現在照 CP 表硬做,有可能做完才發現孩子卡在更前面。

---

## 🚫 刻意不做(連理由一起記,免得下次再議一次)

| 不做 | 理由 |
|---|---|
| **改掉 XP 原版那四個低對比數字色** | 2綠/3紅/6青/8灰對灰底不到 3:1 是**原版本身**的缺點。把紅色的 3 改深就不是踩地雷了,老玩家第一眼就發現。改出「高對比(投影用)」主題給教室與長輩,原味保留 |
| **聖經皮膚 / 經文包裝** | 0916 使用者拍板:這款收在「經典街機合輯」,純經典休閒,不做聖經皮 |
| **手機高級盤硬塞進一個螢幕** | 量過:30×16 不論直向橫向都要捲,而縮到觸控下限以下等於點不中。**沒有解就說沒有解**,畫面直接建議用平板/電腦或改玩中級 |
| **把提示做成「自動解到底」** | 提示是推一把,不是代打。一次只指一格,而且用過就不進紀錄 |
| **鎖橫向** | 這款是**直向友善**的(正方形盤面 + 直向不用捲)。鎖橫向會逼玩家進到比較差的方向。參見合輯 skill `portrait-landscape-dual-layout` |
| **把「新局/說明」放回手機底部列** | 它們在笑臉鈕與說明選單裡各有入口;五顆鈕在 390px 會排兩列、吃掉 100px 的棋盤 |

---

## 🩹 已知問題(不影響玩,但別忘了)

- **repo 住在含中文的路徑**(`…\Downloads\0916早\…`)⇒ Node 24 的 `fs.rm*` 會硬殺進程。
  已用 `scripts/clean-dist.mjs` 繞開,**根治是搬到純 ASCII 路徑**(這台機的每個 repo 都適用)
- **高級盤在手機直向會觸發瀏覽器縮放**(0.73x)⇒ CSS 的 22px 觸控下限到畫面上只剩 16px。
  這是「頁面比視窗寬」的必然,真正的解是待做 #4 的縮放/平移
